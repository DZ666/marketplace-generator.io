{
  "name": "@marketplace/config",
  "version": "1.0.0",
  "description": "Общая библиотека конфигурации для микросервисов маркетплейса",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prebuild": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/config": "^3.1.1",
    "@nestjs/microservices": "^10.0.0",
    "amqp-connection-manager": "^4.1.14",
    "amqplib": "^0.10.3",
    "dotenv": "^16.3.1",
    "joi": "^17.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.1.3"
  },
  "files": [
    "dist/**/*",
    "src/**/*"
  ]
} 